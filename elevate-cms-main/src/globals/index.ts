import { SiteHeader } from '@/globals/SiteHeader';
import { SiteFooter } from '@/globals/SiteFooter';
import { SiteConfig } from '@/globals/SiteConfig';

export const globals = [SiteHeader, SiteFooter, SiteConfig];
